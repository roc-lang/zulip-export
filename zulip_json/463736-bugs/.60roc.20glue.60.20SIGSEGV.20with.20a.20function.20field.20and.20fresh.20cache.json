[
    {
        "content": "<p>[might be a false alarm, I'm new to Roc and am also learning the platform concept for the first time]</p>\n<p>I'm on a MacOS M1, and my platform is written in Rust, not sure if that matters.</p>\n<p><code>roc check --no-cache</code> works, but then <code>roc glue</code> segfaults when a record has a function:</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>  Value :: { first : (U64 -&gt; U64) }.{\n   value : Value\n   value = { first: |x| x }\n  }\n</code></pre></div>\n<p>Reproduced the crash on:</p>\n<p>- <code>nightly-2026-09-05-b195f5b</code><br>\n  - <code>nightly-2026-09-08-39a3f89</code></p>\n<p>Attached is the smallest case I could find to repro this.<br>\nUsing the attachment, you can repro with this:</p>\n<div class=\"codehilite\" data-code-language=\"Bash\"><pre><span></span><code>mkdir<span class=\"w\"> </span>-p<span class=\"w\"> </span>out-failing\nroc<span class=\"w\"> </span>check<span class=\"w\"> </span>--no-cache<span class=\"w\"> </span>failing/platform.roc\n<span class=\"nv\">roc_report_cache</span><span class=\"o\">=</span><span class=\"k\">$(</span>mktemp<span class=\"w\"> </span>-d<span class=\"k\">)</span>\n<span class=\"nv\">ROC_CACHE_DIR</span><span class=\"o\">=</span><span class=\"s2\">\"</span><span class=\"nv\">$roc_report_cache</span><span class=\"s2\">\"</span><span class=\"w\"> </span><span class=\"nv\">XDG_CACHE_HOME</span><span class=\"o\">=</span><span class=\"s2\">\"</span><span class=\"nv\">$roc_report_cache</span><span class=\"s2\">\"</span><span class=\"w\"> </span>roc<span class=\"w\"> </span>glue<span class=\"w\"> </span>--no-cache\nEmptyGlue.roc<span class=\"w\"> </span>out-failing<span class=\"w\"> </span>failing/platform.roc\n</code></pre></div>\n<p>one last tricky bit - reusing a cache accidentally can make this succeed even with <code>--no-cache</code>, so I ended up doing this in a new folder each time I wanted to repro. AI thinks this regression started on September 8th, but I have no idea if that's true or not.</p>\n<p><a href=\"/user_uploads/22008/dL6lHF6izBv7zBW6Vkk7ysUh/roc-glue-repro.tar.gz\">roc-glue-repro.tar.gz</a></p>",
        "id": 622763302,
        "sender_full_name": "Chander Ramesh",
        "timestamp": 1788942482
    },
    {
        "content": "<p>definitely a bug! I'll push a fix later today</p>",
        "id": 622872073,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1788966103
    }
]