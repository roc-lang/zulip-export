[
    {
        "content": "<p>I've just triaged and updated all the remaining issues in basic-cli <a href=\"https://github.com/roc-lang/basic-cli/issues\">https://github.com/roc-lang/basic-cli/issues</a></p>\n<p>There are a few Good First Issues there if anyone is looking for ways to contribute, but maybe more comfortable with Rust or tasks with a smaller scope.</p>",
        "id": 612013651,
        "sender_full_name": "Luke Boswell",
        "timestamp": 1784681521
    },
    {
        "content": "<p>Thanks for the triage — I'd like to start picking these up. I've been working through the <code>List</code>/<code>Str</code> builtins on <a href=\"https://github.com/roc-lang/roc/issues/9596\">#9596</a>, so basic-cli is a new repo for me, but I've got the platform oriented: <code>Host.roc</code> declaration → <code>main.roc</code> hosted entry → generated glue → <code>lib.rs</code>, with the public modules wrapping <code>Host.*</code>.</p>\n<p>Starting with <a href=\"https://github.com/roc-lang/basic-cli/issues/409\">#409 (<code>Sleep.seconds!</code>)</a> as a warm-up — purely additive Roc in <code>Sleep.roc</code> over the existing <code>Host.sleep_millis!</code>, so no host or glue changes.</p>\n<p>One design question before I write it: <code>seconds! : U64 =&gt; {}</code>, consistent with the existing <code>millis!</code>, or <code>F64</code> for fractional sleeps like <code>1.5</code>? I lean <code>U64</code> for consistency — fractional waits are already expressible via <code>millis!</code>. I'd also update <code>examples/time.roc</code>, which currently reads <code>Sleep.millis!(1000)</code> with a <code># 1000 ms = 1 second</code> comment.</p>\n<p>After that I'd look at <a href=\"https://github.com/roc-lang/basic-cli/issues/386\">#386</a> (<code>Path.replace_utf8!</code> — also pure Roc, and per the triage note it wants retitling, since whole-file ops now live on <code>Path</code>), then <a href=\"https://github.com/roc-lang/basic-cli/issues/109\">#109</a> (append), which would be my first pass through the Rust host.</p>\n<p>On <a href=\"https://github.com/roc-lang/basic-cli/issues/223\">#223</a> and <a href=\"https://github.com/roc-lang/basic-cli/issues/162\">#162</a> — the triage notes suggest dropping <code>good first issue</code> given the cross-platform PATH/PATHEXT/<code>OsStr</code> and symlink/canonicalization surface. Agreed, but I'm interested in both once I've got a couple of the smaller ones through.</p>",
        "id": 612604372,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784907088
    },
    {
        "content": "<blockquote>\n<p><code>F64</code> for fractional sleeps like <code>1.5</code>?</p>\n</blockquote>\n<p>I like that for flexibility, so that when you need something a little bit faster or slower you do not need to switch to a different function.</p>",
        "id": 612606325,
        "sender_full_name": "Anton",
        "timestamp": 1784907659
    },
    {
        "content": "<blockquote>\n<p>I'd also update <code>examples/time.roc</code>, which currently reads <code>Sleep.millis!(1000)</code> with a <code># 1000 ms = 1 second</code> comment.</p>\n</blockquote>\n<p>Sounds good!</p>",
        "id": 612606377,
        "sender_full_name": "Anton",
        "timestamp": 1784907680
    },
    {
        "content": "<blockquote>\n<p>I'm interested in both once I've got a couple of the smaller ones through.</p>\n</blockquote>\n<p>Feel free to try and see how it feels :)</p>",
        "id": 612606493,
        "sender_full_name": "Anton",
        "timestamp": 1784907718
    },
    {
        "content": "<p>Thanks <span class=\"user-mention\" data-user-id=\"361169\">@Anton</span>! Opened as <a href=\"https://github.com/roc-lang/basic-cli/pull/460\">#460</a> — <code>seconds! : F64 =&gt; {}</code> over the existing <code>Host.sleep_millis!</code>, no host or glue changes, plus the <code>examples/time.roc</code> swap.</p>\n<p>One thing worth a look that we didn't cover above: the edge cases. <code>F64.round_to_u64</code> crashes on negative, <code>NaN</code>, infinite, and out-of-range values, which doesn't work for a signature with no error channel — so I went with <code>F64.to_u64_try</code> and split the rejected set by sign. Negative and <code>NaN</code> return immediately (matching Go's <code>time.Sleep</code>), and anything past <code>U64.highest</code> ms saturates there.</p>\n<p>Measured against a local platform build: <code>1.5 → 1500 ms</code>, <code>0.25 → 253 ms</code>, <code>-5 → 0 ms</code>, <code>NaN → 0 ms</code>. Full rationale is in the PR description — happy to change the semantics if you'd prefer something else.</p>",
        "id": 612611136,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784909205
    },
    {
        "content": "<p>I don't think rounding should ever crash</p>",
        "id": 612623724,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784913719
    },
    {
        "content": "<p>if we can do it in 1 instruction and that 1 instruction works exactly the same way for all NaN values regardless of bit pattern, we should offer that</p>",
        "id": 612623803,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784913753
    },
    {
        "content": "<p>but if we're doing a conditional on it then we should just offer <code>_try</code> and that's it</p>",
        "id": 612623835,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784913772
    },
    {
        "content": "<p>Looked at the wiring: every <code>round_to_*</code> is <code>out_of_range_or_crash(to_X_try(round(self)))</code> — the crash is just the <code>_try</code>'s <code>Err(OutOfRange)</code> re-wrapped as a <code>crash</code>. And <code>to_X_wrap</code> (total: NaN/±inf → 0, wraps on overflow) and <code>to_X_try</code> (fallible) already sit right next to it.</p>\n<p>The raw hardware truncation isn't uniform across targets for the bad cases (x86 → integer-indefinite, ARM <code>fcvtz*</code> → saturates, wasm <code>trunc</code> → traps), so there's no free \"1 instruction, same for every NaN\" version — a total answer has to pick a policy behind a branch. By your own split that lands on: drop the crashing <code>round_to_*</code> in favor of <code>round_to_*_try</code> (→ <code>Try(Int, [OutOfRange])</code>), leaving <code>_wrap</code> as the explicit total escape hatch. Making <code>round_to_*</code> silently saturate would bake a clamp policy into a \"total\" signature, which feels less Roc than making the partiality explicit.</p>\n<p>If that's the direction, happy to take it as a pure-Roc PR — swapping the <code>out_of_range_or_crash</code> wrapper for the <code>Try</code> across the <code>round_to_*</code> family + snapshots. Or would you want a saturating variant too?</p>",
        "id": 612633667,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784916695
    },
    {
        "content": "<p>no let's just do the _try ones</p>",
        "id": 612637507,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784918145
    },
    {
        "content": "<p>Before I open a PR: the crash isn't just <code>round_to_*</code> — <code>floor_to_*</code> and <code>ceiling_to_*</code> route through the same <code>out_of_range_or_crash</code> helper, so it's 90 fns (10 int types × Dec/F32/F64), not the ~30 I said. I've prototyped all three as <code>&lt;op&gt;_to_&lt;int&gt;_try : Frac -&gt; Try(Int, [OutOfRange])</code> (helper dropped; nice bonus — the out-of-range/NaN path is finally testable). Two calls for you: all three in one PR or split by op, and naming as <code>round_to_i8_try</code> (mirrors <code>to_i8_try</code>)?</p>",
        "id": 612650976,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784923939
    },
    {
        "content": "<p>one pr is fine <span aria-label=\"+1\" class=\"emoji emoji-1f44d\" role=\"img\" title=\"+1\">:+1:</span></p>",
        "id": 612657373,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784926315
    },
    {
        "content": "<p>and that naming is fine too</p>",
        "id": 612657445,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1784926341
    },
    {
        "content": "<p>Opened as <a href=\"https://github.com/roc-lang/roc/pull/10360\">https://github.com/roc-lang/roc/pull/10360</a> — all three families (round/floor/ceiling) → <code>_try</code>, one PR. Thanks for the quick calls!</p>",
        "id": 612662601,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784928131
    }
]