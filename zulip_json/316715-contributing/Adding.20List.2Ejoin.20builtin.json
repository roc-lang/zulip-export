[
    {
        "content": "<p>Hi! I'm new here and getting familiar with the new Zig compiler (built it from source on zig 0.16.0). I'd like to pick up a small builtin from <a href=\"https://github.com/roc-lang/roc/issues/9596\">#9596</a> as a first contribution: <code>List.join : List(List(a)) -&gt; List(a)</code> — the one Anton-4 flagged in the issue comment (<code>[[1, 2], [3], [], [4, 5]].join() == [1, 2, 3, 4, 5]</code>).</p>\n<p>I've got a draft ready — implemented in pure Roc in <code>Builtin.roc</code> on top of <code>List.fold</code> + <code>List.concat</code>, with <code>## expect</code> doctests and a repl snapshot under <code>test/snapshots/</code>. Before I open a PR: is that approach OK, would you want capacity reservation for performance, and any preference on snapshot type (expr vs repl)? Thanks!</p>",
        "id": 611009629,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784189540
    },
    {
        "content": "<p>Quick follow-up before the PR: the naive <code>fold</code> + <code>concat</code> version is actually O(n²) — <code>List.concat</code> reserves exactly <code>len_a + len_b</code>, and <code>calculateCapacity</code> only grows geometrically on <code>+1</code> appends, so the accumulator gets reallocated and the prefix recopied each step. Going two-pass instead: sum the lengths, <code>List.with_capacity(total)</code>, then <code>concat</code> each sublist in — one allocation, O(n).</p>",
        "id": 611112464,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784218804
    },
    {
        "content": "<p>Opened it as a draft: <a href=\"https://github.com/roc-lang/roc/pull/10179\">https://github.com/roc-lang/roc/pull/10179</a> — went with the two-pass <code>with_capacity</code> version (O(n)). Left it as draft for now; feedback on the snapshot type or anything else very welcome. <span aria-label=\"folded hands\" class=\"emoji emoji-1f64f\" role=\"img\" title=\"folded hands\">:folded_hands:</span></p>",
        "id": 611144988,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784228391
    },
    {
        "content": "<p>Correction on <code>List.join</code>: my O(n) claim above was wrong, and I've pushed a fix.</p>\n<p><code>List.with_capacity(total)</code> + <code>List.concat</code> in a loop is <em>not</em> O(n). <code>listConcat</code> early-returns when its first argument is empty, and <code>isEmpty()</code> checks length, not capacity — so on the first iteration the reserved buffer is just freed and <code>list_b</code> is returned. After that the accumulator has <code>capacity == length</code> at every step, so each <code>concat</code> reallocates and recopies the prefix. My \"optimized\" version was exactly as quadratic as the naive <code>fold</code> + <code>concat</code> it was meant to beat — on <code>List.join(List.repeat([1, 2], N))</code> at N = 50k that's 14.5 s, against 0.24 s for the fixed version.</p>\n<p>Fixed by filling the reservation with <code>list_append_unsafe</code>, the same way <code>List.rev</code> / <code>List.map</code> / <code>List.from_iter</code> do. Details and numbers in <a href=\"https://github.com/roc-lang/roc/issues/10179\">#10179</a>.</p>",
        "id": 611241787,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784278607
    },
    {
        "content": "<p>Windows <code>zig-minici</code> on <a href=\"https://github.com/roc-lang/roc/issues/10179\">#10179</a> timed out rather than failed: the job was <strong>cancelled</strong> at 2h00m34s against its 120-minute limit, with <code>Run MiniCI</code> cancelled mid-step. macOS and Linux both passed, and local <code>zig build minici</code> is 62/62. It took 1h54m on the previous head, so it's been running close to the ceiling.</p>\n<p>I can't re-run it from a fork — could someone kick that job whenever it's convenient? <span aria-label=\"folded hands\" class=\"emoji emoji-1f64f\" role=\"img\" title=\"folded hands\">:folded_hands:</span></p>",
        "id": 611267080,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784287753
    },
    {
        "content": "<p>Update: <a href=\"https://github.com/roc-lang/roc/issues/10179\">#10179</a> is green on all three platforms now and I've marked it ready for review. <span aria-label=\"folded hands\" class=\"emoji emoji-1f64f\" role=\"img\" title=\"folded hands\">:folded_hands:</span></p>",
        "id": 611347784,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784312099
    }
]