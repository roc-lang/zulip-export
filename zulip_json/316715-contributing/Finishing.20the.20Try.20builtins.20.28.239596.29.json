[
    {
        "content": "<p><code>List.join</code> aside, the <code>## Try Builtins</code> section of <a href=\"https://github.com/roc-lang/roc/issues/9596\">#9596</a> is fully open — nine entries. If nobody's on them, I'd like to take the section:</p>\n<div class=\"codehilite\"><pre><span></span><code>Try.map_both / map_both!    Try.map2 / map2!\nTry.on_err   / on_err!      Try.catch / catch!      Try.collapse\n</code></pre></div>\n<p>All pure Roc with an inline <code>match</code>, mirroring the existing <code>map_ok</code> / <code>map_ok!</code> / <code>map_err</code> / <code>map_err!</code> — no new low-level op. <code>collapse</code> is just <code>catch</code> with identity functions, so those two want to land together.</p>\n<p>One note on the signatures: the issue writes <code>map_both : Try(ok1, err1), (ok1 -&gt; ok2), …</code>, but <code>map2</code> in the same list uses <code>a</code>/<code>b</code>/<code>c</code>, and <code>Builtin.roc</code> doesn't number type variables anywhere. I'd follow the existing convention — <code>map_err : Try(ok, a), (a -&gt; b) -&gt; Try(ok, b)</code> — and write e.g. <code>on_err : Try(ok, a), (a -&gt; Try(ok, b)) -&gt; Try(ok, b)</code>. Shout if you'd rather keep the issue's spelling.</p>\n<p>Is anyone already on these, and is the whole section one PR, or would you rather have it split?</p>",
        "id": 611259723,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784284956
    },
    {
        "content": "<p>Whole section PR is fine. I dont think anyone has mentioned working on those.</p>\n<p>If you wanted to split them across commits that would be nice.</p>",
        "id": 611261760,
        "sender_full_name": "Luke Boswell",
        "timestamp": 1784285822
    },
    {
        "content": "<p>Thanks! One PR, one commit per pair — each <code>!</code> variant with its sibling.</p>",
        "id": 611267868,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784288018
    },
    {
        "content": "<p><a href=\"https://github.com/roc-lang/roc/issues/10188\">#10188</a> is green on all three platforms and marked ready for review. <span aria-label=\"folded hands\" class=\"emoji emoji-1f64f\" role=\"img\" title=\"folded hands\">:folded_hands:</span></p>",
        "id": 611414798,
        "sender_full_name": "Dzmitry Misiuk",
        "timestamp": 1784355717
    }
]