[
    {
        "content": "<p>My understanding is that <code>from_quote</code> is always executed at compile time, and if it compiles successfully, we can entirely ignore the error case. Is that correct?</p>\n<p>If so, are the following basically equivalent (they don't call the same function but they do the same thing)?</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>d: Date\nd = \"2018\"  # actually calls Date.from_quote(\"2018\")\n\nOk(d) = Date.from_str(\"2018\")\n</code></pre></div>\n<p>Why do we need both <code>from_quote</code> and <code>from_str</code>? Couldn't we get away with a single function?</p>\n<p>Also, why must <code>from_quote</code> return <code>Try(_, [BadQuotedBytes(Str)])</code>? Couldn't it allow any other error?</p>\n<p>For context, there's an Exercism exercise where the tests look like this:</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>expect {\n    result = shared_birthday([\"1966-07-29\", \"1977-02-12\", \"2001-12-25\", \"1980-11-10\"])\n    result == Ok(Bool.False)\n}\n</code></pre></div>\n<p>The function must simply parse the strings and return <code>True</code> if any two dates share the same day &amp; month. The type spec of the <code>shared_birthday</code> function could be:</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>shared_birthday : List(Str) -&gt; Try(Bool, _)\n</code></pre></div>\n<p>The user would have to implement this function and take care of parsing the dates (returning an error in case of a parsing error).</p>\n<p>But it might be more instructive and more idiomatic to define it as:</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>Date :: {year: I64, month: U8, day: U8}.{\n    from_quote : Str -&gt; Try(Date, [BadQuotedBytes(Str)])\n    ...\n}\n\nshared_birthday : List(Date) -&gt; Bool\n</code></pre></div>\n<p>In this case the test above would look like this instead:</p>\n<div class=\"codehilite\" data-code-language=\"roc\"><pre><span></span><code>expect {\n    shared_birthday([\"1966-07-29\", \"1977-02-12\", \"2001-12-25\", \"1980-11-10\"])\n}\n</code></pre></div>\n<p>Wdyt?</p>",
        "id": 626795073,
        "sender_full_name": "Aurélien Geron",
        "timestamp": 1790314148
    }
]