[
    {
        "content": "<p>I'm not sure why the following evaluates at compile time</p>\n<div class=\"codehilite\"><pre><span></span><code>Ok(v) = (0xFF.U32).to_u8_try()\n</code></pre></div>\n<p>but attempting the following gives a non exhaustive destructuring error</p>\n<div class=\"codehilite\"><pre><span></span><code>to_u8_clamp = |limit| {\n    match limit.to_u8_try() {\n        Ok(limit_u8) =&gt; {\n            Ok(\n                |v| {\n                    if v &lt; limit v.to_u8_wrap() else limit_u8\n                },\n            )\n        }\n        Err(_) =&gt; Err(LimitTooBig)\n    }\n}\n\nf = || {\n    Ok(converter) = to_u8_clamp(0xFF.U32)  # &lt;- Error here\n    converter(120)\n}\n</code></pre></div>\n<p><code>to_u8_clamp</code> is a pure function using the same underlying <code>to_u8_try</code>, so I would have thought that the same compile time evaluation and destructuring would be possible.</p>",
        "id": 615458716,
        "sender_full_name": "Jonathan",
        "timestamp": 1786279870
    },
    {
        "content": "<p>Just for testing, if <code>limit</code> is set inside the function instead, I get a warning that the value being matched is known at compile time, but I still can't destructure the result like <code>Ok(converter) = </code>.</p>\n<div class=\"codehilite\"><pre><span></span><code>testing = || {\n    limit = 0xFF.U32\n    match limit.to_u8_try() {    #  &lt;- Warning: this value is known at compile time\n        Ok(limit_u8) =&gt; {\n            Ok(\n                |v| {\n                    if v &lt; limit v.to_u8_wrap() else limit_u8\n                },\n            )\n        }\n        Err(_) =&gt; Err(LimitTooBig)\n    }\n\n}\n\nmain! = |_| {\n    Ok(f) = testing() # &lt;- Error: Non Exhaustive Destructuring\n    Ok({})\n}\n</code></pre></div>\n<p>The warning doesn't say explicitly, however, that \"the Ok(...) branch will always match\". (If that's relevant)</p>",
        "id": 615459667,
        "sender_full_name": "Jonathan",
        "timestamp": 1786280750
    },
    {
        "content": "<p>this seems like a bug - can you open a GH issue?</p>",
        "id": 615471303,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1786286601
    },
    {
        "content": "<p>Sorry for the delay, yes sure! <a href=\"https://github.com/roc-lang/roc/issues/10721\">#10721</a></p>",
        "id": 615585053,
        "sender_full_name": "Jonathan",
        "timestamp": 1786355175
    },
    {
        "content": "<p>Opened <a href=\"https://github.com/roc-lang/roc/issues/10722\">#10722</a> too</p>",
        "id": 615586753,
        "sender_full_name": "Jonathan",
        "timestamp": 1786355806
    }
]