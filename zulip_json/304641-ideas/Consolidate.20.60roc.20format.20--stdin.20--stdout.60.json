[
    {
        "content": "<p>Right now we have <code>--stdin</code> and <code>--stdout</code> for <code>roc format</code>:</p>\n<div class=\"codehilite\"><pre><span></span><code>$ roc format --help\n...\n      --stdin    Read file to format from stdin\n      --stdout   Print formatted file to stdout\n</code></pre></div>\n<p>I propose we merge these into a single flag called <code>--stdin</code> that reads from stdin and writes to stdout. I suspect this is what people would want most of the time. This is also what Zig does:</p>\n<div class=\"codehilite\"><pre><span></span><code>$ zig fmt --help\n...\n--stdin                Format code from stdin; output to stdout\n</code></pre></div>\n<p>If you need more control you can always still pipe things:</p>\n<div class=\"codehilite\"><pre><span></span><code>cat input.roc | roc format --stdin\nroc format --stdin &gt; output.roc\n</code></pre></div>\n<p>This is a way that we can slightly decrease the surface area of the CLI which always seems nice to me <span aria-label=\"smile\" class=\"emoji emoji-1f642\" role=\"img\" title=\"smile\">:smile:</span></p>",
        "id": 504442005,
        "sender_full_name": "Isaac Van Doren",
        "timestamp": 1741562163
    },
    {
        "content": "<p>What does stdin without stdout even do?</p>",
        "id": 504442748,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1741562786
    },
    {
        "content": "<p>And yeah, sounds good to merge</p>",
        "id": 504442766,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1741562805
    },
    {
        "content": "<p><code>roc format foo.roc --stdout</code> prints the formatted version of <code>foo.roc</code> to stdout. It looks like <code>roc format --stdout</code> is a noop (I haven't actually looked at the code). I'm not sure if there's anythign reasonable for it to do</p>",
        "id": 504445839,
        "sender_full_name": "Isaac Van Doren",
        "timestamp": 1741565333
    }
]