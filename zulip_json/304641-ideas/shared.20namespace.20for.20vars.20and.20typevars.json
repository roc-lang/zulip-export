[
    {
        "content": "<p>What if roc had shared namespace for vars and typevars? I'm going off the fact that type names are already can be used in the values world explicitly: <code>Type.[exported thing from the Type module]</code>, e.g. <code>List.map(list, fn)</code>. <code>Type</code> there is not a record but a namespace. The idea is to allow the namespace be taken from the types annotation.</p>\n<div class=\"codehilite\"><pre><span></span><code># type namespace\ndecode_json = |bytes| {\n    Json.decode(bytes)\n}\n\n# typevar namespace\ndecode_anonymous : List(U8) -&gt; Result(dest, [DecodeErr])\ndecode_anonymous = |bytes| {\n    dest.decode(bytes)\n}\n</code></pre></div>\n<p>It means now outer space affects typevars naming, but it might be a pro since now there's no confusion about possibly same names in type annotations and value variables.</p>\n<p>The <code>decode</code> problem is not only about the return type, but more generally, about the ability to express static methods (or any exports) of an inferred type. Static methods are functions exported from a module that don't have the module type as a first argument. E.g <code>Type.from_digits</code></p>\n<div class=\"codehilite\"><pre><span></span><code>formula : num, num -&gt; num\nformula = |radius| {\n    3.14.num * radius * radius\n}\n</code></pre></div>\n<p>One of the things that the module can export is the type constructor: <code>Type(...)</code>. It's possible to use this with typevar as well, but likely never needed.</p>\n<p>There's also syntax idea for type clarification syntax. The same way type annotation is <code>var : type</code>, it might be extended to <code>expr : type</code>:</p>\n<div class=\"codehilite\"><pre><span></span><code>decode_anonymous = |bytes| {\n    dest.decode(bytes) : Result(dest, [DecodeErr])\n}\n</code></pre></div>",
        "id": 543179153,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1759685066
    },
    {
        "content": "<p>hm, what would happen if you just returned a type variable like <code>dest</code>?</p>",
        "id": 543185591,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1759690704
    },
    {
        "content": "<p>A constructor function I think. I assumed that in the message above, but now I'm not sure if it's possible to do. It seems constructors are allowed only within the type module scope? In such case, the rule is the same as for the <code>Type</code>: if it cannot be considered as a function, only as a namespace - it shouldn't be possible to use it as value.</p>",
        "id": 543186653,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1759691670
    },
    {
        "content": "<p>what is a constructor function? <span aria-label=\"sweat smile\" class=\"emoji emoji-1f605\" role=\"img\" title=\"sweat smile\">:sweat_smile:</span></p>",
        "id": 543191612,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1759696763
    },
    {
        "content": "<p>The type wrapper. As I said, it's useless. It only makes sense for tags.<br>\nAnyway, the point is that if you return a typevar from a function - you get the same outcome as if you returned <code>List</code> or <code>U64</code> from the function</p>",
        "id": 543193679,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1759699284
    },
    {
        "content": "<p>hm, I dunno - seems pretty inconvenient to not be able to write things like <code>|list, elem|</code> in the implementation of <code>append</code> because <code>elem</code> would be shadowed</p>",
        "id": 543195508,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1759701537
    },
    {
        "content": "<p>and it's to accommodate an edge case</p>",
        "id": 543195516,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1759701549
    }
]