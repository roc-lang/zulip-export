[
    {
        "content": "<p>I think we should merge <code>Num.pow</code> and <code>Num.powInt</code>.<br>\nIt would just be <code>Num.pow : Num a, Num a -&gt; Num a</code></p>\n<p>My main reasons why:</p>\n<ul>\n<li>Roc has a type system that can generate the correct version of <code>Num.pow</code> based on type. Just like we do with most <code>Num</code> builtins.</li>\n<li>This is the only function that is split this way. There is no other <code>Num.somethingInt</code> function in the standard.</li>\n<li>Both functions fundamentally do the same thing, so I don't think there would be any surprises if they are merged.</li>\n</ul>\n<p>Only con off the top of my head:</p>\n<ul>\n<li><code>pow</code> on Integers is more likely to overflow (which causes a panic in roc if not checked). So having explicitly <code>powInt</code> would make it easier to identify these likely overflow cases.</li>\n</ul>\n<p>Given we will have <code>powChecked</code> and users can always cast to larger types or to floats if they want to avoid overflow, I personally don't see this as a big concern.</p>",
        "id": 363099105,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1685722187
    },
    {
        "content": "<p>If we can generate based on type it's all good by me :)</p>",
        "id": 363117817,
        "sender_full_name": "Anton",
        "timestamp": 1685726300
    },
    {
        "content": "<p>I thought Num.powInt was separated because it needs to produce an err if you write Num.powInt 2 -1 (otherwise you have Elms problem where this function says it returns an int but actually it’s a float)</p>",
        "id": 363241593,
        "sender_full_name": "Martin Stewart",
        "timestamp": 1685781765
    },
    {
        "content": "<p>Good point!</p>",
        "id": 363255474,
        "sender_full_name": "Anton",
        "timestamp": 1685785422
    },
    {
        "content": "<p>That would be a very intelligent reason to separate <code>pow</code> from <code>powInt</code>. In our current cases, <code>powInt</code> does not return a result. Instead, on negative values, it just returns <code>1</code> (Just learned this by testing in the  repl).</p>",
        "id": 363313273,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1685800689
    },
    {
        "content": "<p>Oh, also, powInt is broken. If the value overflows, it just returns the initial number. So <code>Num.powInt 2 200</code> is <code>2</code> (I'll file a bug for that).</p>",
        "id": 363313683,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1685800815
    },
    {
        "content": "<p>Two thoughts on options with context around negative numbers:</p>\n<ol>\n<li>Just make <code>Num.pow</code> on integers, with a negative power that would return a fraction, truncate just like int operations normally do. So <code>Num.pow 2u32 -1</code> would just return <code>0</code>. Cause that is the correct answer as an integer.</li>\n<li>Keep around <code>Num.powInt</code>, but require the second value to be a <code>U8</code>. That gets rid of the negative number problem and a <code>U8</code> can store enough values that it can overflow any integer type even when raising 2 to a power.</li>\n</ol>",
        "id": 363314744,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1685801134
    }
]