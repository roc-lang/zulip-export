[
    {
        "content": "<p>Chaining got me thinking. Maybe there is a better way to express the flow and error cases.</p>\n<p>My main issue with the default implementation given for <code>!</code> as <code>|&gt; Task.await</code> is that you lose errors.<br>\nAll errors essentially are treated as exceptions that are pushed off for the wrapping function to deal with.</p>\n<p>Personally, I don't like this implicit loss of local error control.</p>\n<p>I think we might create a nicer api overall by keeping the results around and instead adding<br>\nfunctionality to pass errors up the stack in an early return sort of a way.</p>\n<p>I would prefer to be able to do this:</p>\n<div class=\"codehilite\" data-code-language=\"CoffeeScript\"><pre><span></span><code><span class=\"c1\"># No error case, just get x directly</span>\n<span class=\"nv\">x</span><span class=\"w\"> </span><span class=\"o\">=</span><span class=\"w\"> </span><span class=\"nx\">Stdin</span><span class=\"p\">.</span><span class=\"nx\">line</span><span class=\"o\">!</span>\n\n<span class=\"nx\">Stdout</span><span class=\"p\">.</span><span class=\"nx\">line</span><span class=\"o\">!</span><span class=\"w\"> </span><span class=\"nx\">x</span>\n\n<span class=\"c1\"># This has an error case we get the full result.</span>\n<span class=\"nv\">res</span><span class=\"w\"> </span><span class=\"o\">=</span><span class=\"w\"> </span><span class=\"nx\">File</span><span class=\"p\">.</span><span class=\"nx\">readBytes</span><span class=\"o\">!</span><span class=\"w\"> </span><span class=\"nx\">path</span>\n\n<span class=\"c1\"># We don't want to handle the error locally though</span>\n<span class=\"nv\">bytes</span><span class=\"w\"> </span><span class=\"o\">=</span><span class=\"w\"> </span><span class=\"nx\">res</span><span class=\"o\">?</span>\n</code></pre></div>\n<p>In the above example, <code>!</code> was chosen to map to <code>Task.attempt</code> and will always bring the error case locally.<br>\n<code>?</code> was chosen as an explicit unwrap mechanism.<br>\nIt will lead to any errors early returning such that the local situation can ignore the error.<br>\nI guess if really wanted, we could use <code>!?</code> to merge the two behaviours together in one location.</p>\n<p>Note: <code>!</code> and <code>?</code> could be symbols or keywords. I am trying to avoid the exact syntactical assessment and focus more on the differences from <a class=\"stream-topic\" data-stream-id=\"304641\" href=\"/#narrow/stream/304641-ideas/topic/chaining.20syntax\">#ideas &gt; chaining syntax</a> in terms of error behavior and propagation.</p>",
        "id": 411822660,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704750965
    },
    {
        "content": "<p>A quick not on how this applies to other interafaces.</p>\n<p>For result, you would never need <code>!</code>, you would just use <code>?</code> to send the error.</p>\n<p>For things link an rng or other various implementations, we could theoretically expand <code>!</code> to still use <code>with</code> and support them. They would just always succeed without an error case and would not need to be used with <code>?</code></p>",
        "id": 411823051,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704751162
    },
    {
        "content": "<p>Also, before someone asks what all the types are to make this work, they don't work. <code>!</code> is technically two different functions depending on if a task can return an error or not. That is for user convenience and I think would be worth adding compiler magic. Otherwise, there would be many <code>?</code> for types that don't even have an error case. It is just clutter.</p>",
        "id": 411823396,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704751295
    },
    {
        "content": "<p>I really like this idea. <br>\nParticularly the <code>!</code> <code>?</code> <code>!?</code> combo, I think it's super natural and intuitive<br>\nI was a little confused by your mention of Task.attempt  and why that's even necessary and I realized I actually don't really understand why tasks that can fail don't return a result, could you point me to an explanation of that?</p>",
        "id": 411825735,
        "sender_full_name": "Eli Dowling",
        "timestamp": 1704752331
    },
    {
        "content": "<p>The core difference is in how <a href=\"https://www.roc-lang.org/packages/basic-cli/Task#attempt\">Task.attempt</a> and <a href=\"https://www.roc-lang.org/packages/basic-cli/Task#await\">Task.await</a> handle the error case.</p>\n<p><code>Task.await</code> only cares about the happy path. Essentially, if everything is good, continue like so.<br>\nAs such, all possible error cases will be aggregated together into one gigantic error. That entire gigantic error is what will be returned and left for the calling function to deal with.</p>\n<p><code>Task.attempt</code> on the other hand deals with both paths together. It gives a result that contains the happy path and the potential errors.</p>\n<p>Talking in terms of the syntax shown above, <code>Task.attempt</code> is <code>!</code>. <code>Task.await</code> is <code>!?</code>.</p>",
        "id": 411826384,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704752608
    },
    {
        "content": "<p><span class=\"user-mention silent\" data-user-id=\"343810\">Brendan Hansknecht</span> <a href=\"#narrow/stream/304641-ideas/topic/chaining.20but.20with.20.60Task.2Eattempt.60.20and.20.60.3F.60.20for.20error.20returns/near/411823396\">said</a>:</p>\n<blockquote>\n<p>Also, before someone asks what all the types are to make this work, they don't work. <code>!</code> is technically two different functions depending on if a task can return an error or not. That is for user convenience and I think would be worth adding compiler magic.</p>\n</blockquote>\n<p>I don't think this compiler magic works without changing Roc's type system somehow; otherwise, what happens if I put this into the repl?</p>\n<div class=\"codehilite\"><pre><span></span><code>» \\arg -&gt;\n    x = arg!\n    x\n</code></pre></div>\n<p>in the <a class=\"stream-topic\" data-stream-id=\"304641\" href=\"/#narrow/stream/304641-ideas/topic/chaining.20syntax\">#ideas &gt; chaining syntax</a> proposal, the repl would print:</p>\n<div class=\"codehilite\"><pre><span></span><code>&lt;function&gt; : Task a b -&gt; Task a b\n</code></pre></div>\n<p>what would it print with this compiler magic?</p>",
        "id": 411835471,
        "sender_full_name": "Richard Feldman",
        "timestamp": 1704757319
    },
    {
        "content": "<p>I mean if we can't do the magic, just means that it has to be <code>!?</code> even if a task has no possible errors.</p>\n<p>I guess that's the cost for wrapping everything in task even when It could just be a raw effect</p>",
        "id": 411836205,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704757787
    },
    {
        "content": "<p>Though maybe there is a nicer API we could make just on effect without the task wrapping.</p>",
        "id": 411836456,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704757926
    },
    {
        "content": "<p>Not really sure. Would definitely need to mess with types.</p>",
        "id": 411836499,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1704757948
    }
]