[
    {
        "content": "<p>I keep thinking about the problem of how to design the optional fields that is being discussed in multiple topics. I'm currently on mobile and haven't kept up with every detail, so hopefully the following idea is new and hasn't yet been discussed, but I wanted to share it before I forget it.</p>\n<p>As far as I understand the only place we want to allow optional fields is for function calls. Records themselves shouldn't really have optional fields, only when they are used to provide named arguments to functions. The current proposals (that I've seen) tie the syntax for optional fields to the general record syntax, so that it can principally also be used outside of function calls.</p>\n<p>That led me to think about whether there is a way to tie the optional fields syntactically to function calls so that they cannot be used elsewhere. I jad this idea:</p>\n<p>\\argument ? { optionalField: 2 } -&gt;<br>\n  sum = argument.requiredField + argument.optionalField</p>\n<p>The type could be displayed as { requiredField : U8, optionalField ? U8 } as before.</p>\n<p>This syntax could be restricted to only function calls. That means using ? outside of a function call throws a compiler error.</p>",
        "id": 425761551,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710065675
    },
    {
        "content": "<p>Looking at this typed out, the current optional field syntax could be kept to allow destructing the arguments, as in \\{ requiredField, optionalField ? 2 }. The main difference is that we would restrict it to only function calls ans throw an error if used elsewhere. </p>\n<p>If we also want to allow using optional fields without forcing destructuring, we could keep the above syntax. Alternatives would be \\{ argument ? optionalField: 2 } or maybe \\{ argument &amp; optionalField ? 2 }, though the latter changes the meaning of the existing syntax undesirably, I think.</p>",
        "id": 425761564,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710065690
    },
    {
        "content": "<p>I guess the main idea is to find a way to deliberately restrict optional fields to function calls. <span aria-label=\"blush\" class=\"emoji emoji-1f60a\" role=\"img\" title=\"blush\">:blush:</span></p>",
        "id": 425761598,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710065738
    },
    {
        "content": "<p>One of the things I was thinking about is to require passed optional fields to be statically known. So they would be a kind of function extension/specialization and as a result - no optional fields in records are needed. Sounds very similar to your suggestion.</p>\n<p>Though not sure about the ergonomics. I guess the point is if the records can’t have optional fields - then there’s no way to pass arbitrary config because each call would require a fixed interface. On the one hand, it can encourage writing more readable explicit function calls. But on the other hand, the dynamic pass would become more verbose and the ergonomics may suffer</p>",
        "id": 425762913,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710067088
    },
    {
        "content": "<p>Do you mean that it would be helpful if the documentation and compiler showed the type as { requiredField : U8, optionalField ? U8 = 2 }?</p>",
        "id": 425763370,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710067606
    },
    {
        "content": "<p>I mean, if records have no optional fields, what would be the type of list of configs, for example?</p>\n<div class=\"codehilite\"><pre><span></span><code>add = \\{ x, y ? 1 } -&gt; x + y\nconfigs = [{x:1, y:2}, {x:1}] # what is the type?\nList.map configs add\n</code></pre></div>",
        "id": 425765120,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710069289
    },
    {
        "content": "<p>I'd say that wouldn't be allowed because a list must contain elements of the same type and the records are incompatible. (The rule is tjat ? is only allowed in function arguments.)</p>",
        "id": 425765351,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710069488
    },
    {
        "content": "<p>But you could call add with either element explicitly.</p>",
        "id": 425765394,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710069551
    },
    {
        "content": "<p>Exactly. So to overcome it one would have to use tags or fill the records with their own defaults explicitly.</p>",
        "id": 425765401,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710069567
    },
    {
        "content": "<p>Yes. What is your concern with that?</p>",
        "id": 425765468,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710069627
    },
    {
        "content": "<p>E. g. if you once added another config with an extra field to the list - you have to fill other configs with defaults to match the interface.</p>\n<p>I use to be on the strict side, but the absence of optional fields in records could bring complicated rituals around them. I’m curious what others think though.</p>",
        "id": 425783878,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710086186
    },
    {
        "content": "<p>Couldn't you refactor the code to provide one record with your common default values and then override the fields in the list?</p>\n<p>By the way, I feel that the list of configs is not the usual way of how functions should be called. So I'm concerned that we give too much emphasis on a case that is uncommon and where it is ok to have minor inconveniences such as having to define the defaults yourself. <span aria-label=\"wink\" class=\"emoji emoji-1f609\" role=\"img\" title=\"wink\">:wink:</span></p>",
        "id": 425801279,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710100974
    },
    {
        "content": "<p>I feel like the devil’s advocate <span aria-label=\"grimacing\" class=\"emoji emoji-1f62c\" role=\"img\" title=\"grimacing\">:grimacing:</span><br>\nIt might be not a list but the result of <code>when</code> or <code>ifelse</code></p>",
        "id": 425806010,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710104999
    },
    {
        "content": "<p>It's interesting!</p>\n<p>Could you help me out with another example? I don't see what if or when would change... <span aria-label=\"sweat smile\" class=\"emoji emoji-1f605\" role=\"img\" title=\"sweat smile\">:sweat_smile:</span></p>",
        "id": 425806361,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710105291
    },
    {
        "content": "<div class=\"codehilite\"><pre><span></span><code>f = \\x -&gt;\n  cfg = if x &gt; 0 then { x, y: x } else { x } # what’s the type?\n  add cfg\n</code></pre></div>\n<p>I know, a made-up example. The point is, you have to add an explicit value <code>y</code> in the <code>else</code> branch even if you actually want this value to be exactly default. The example might be rewritten this way tho:</p>\n<div class=\"codehilite\"><pre><span></span><code>f = \\x -&gt;\n  if x &gt; 0 then\n    add { x, y: x }\n  else\n    add { x }\n</code></pre></div>\n<p>But in my opinion it’s an ergonomics tradeoff.</p>\n<p>Also, what about bypassing the config downstream? If there’s no defaults in records, then internal calls will never apply their defaults as they’re overwritten upstream.</p>",
        "id": 425808077,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710106759
    },
    {
        "content": "<p>I'm still having trouble understanding what you're getting at. The examples you show currently don't compile, they wouldn't either with the idea I propose, and we seem to agree that these examples are edge cases and the main usage is direct function calling. Am I missing something? <span aria-label=\"blush\" class=\"emoji emoji-1f60a\" role=\"img\" title=\"blush\">:blush:</span></p>\n<p>I also didn't understand what you mean with bypassing the config, I think I'd need another example. <span aria-label=\"sweat smile\" class=\"emoji emoji-1f605\" role=\"img\" title=\"sweat smile\">:sweat_smile:</span></p>",
        "id": 425811327,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710109817
    },
    {
        "content": "<p>You're examples were really helpful, by the way!</p>",
        "id": 425811342,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710109837
    },
    {
        "content": "<p>Ha. I was sure this should compile, but it was hard to check from mobile:</p>\n<div class=\"codehilite\"><pre><span></span><code>add = \\{ x, y ? 1 } -&gt; x + y\nf = \\x -&gt;\n  cfg : { x : a, y ? a }\n  cfg = if x &gt; 0 then { x, y : x } else { x }\n  add cfg\n\n(f 1, f -1)\n</code></pre></div>\n<p>It seems not intuitive to me though. Probably a bug, but I apologize for not working examples.</p>\n<p>Anyway, speaking of the bypassing:</p>\n<div class=\"codehilite\"><pre><span></span><code>internal = \\{ x ? 1 } -&gt; x\nexternal = \\arg -&gt; internal arg\n(external {})\n</code></pre></div>\n<p>Currently, it returns the expected <code>1</code><br>\nWhat is the type of <code>arg</code> with lack of optionals in records? Isn’t it have to be a record with an optional field? Since it’s forbidden, we have to destruct the arg first:</p>\n<div class=\"codehilite\"><pre><span></span><code>internal = \\{ x ? 1 } -&gt; x\nexternal = \\{ x ? 2 } -&gt; internal { x }\n</code></pre></div>\n<p>But this way default field from <code>internal</code> will be overwritten so there’s no way to bypass <em>the absence</em> of <code>x</code>.</p>\n<p>My idea was having a syntax like</p>\n<div class=\"codehilite\"><pre><span></span><code>markdownFmt@{ color ? Str, bold ? Bool } : Str -&gt; Str\nmarkdowbFmt@{ color ? &quot;black&quot;, bold ? Bool.false } = \\str -&gt; …\n\n# destruct on demand\nmarkdowbFmt@config = \\str -&gt;\n  { color ? &quot;black&quot;, bold ? Bool.false } = config\n  …\n\n# direct parametrization\nmarkdownFmt@{ color : &quot;red&quot; } &quot;Apple”\n\n# bypass from parameteizarion\ng@config = \\str -&gt; f@config str\n\n# bypass from args, so a record can be converted to a param record if their types are the same\ng = \\str, config -&gt; f@config str\n\n# but this is not possible because the params record can’t be converted to a regular record\ng@config -&gt; h config\n\n# indirect parameter pass\nf = \\str, bold -&gt; markdownFmt@{ bold } str\n\n# indirect config pass\nconfig = { bold : Bool.true }\nf@config &quot;Roc&quot;\n\n# fully default\nmarkdownFmt &quot;Roc&quot;\n\n# it&#39;s even possible to empathize that defaults are used\nmarkdownFmt@{} &quot;Roc&quot;\n\n# range\nList.range@{ step: 3 } (At 0) (Length 100)\n</code></pre></div>\n<p>Where <code>@{ … }</code> is the function parametrization record (plain of course). This way no need to tie optional fields to a specific argument and enforce destructure. With a special syntax, you always know where defaults are overwritten. With this design optional fields in records are not needed. Extra functionality is decoupled from the mandatory interface.</p>\n<p>I like how it feels, but it has almost the same problems with rituals I think (in particular, in the case of a list of configs / branching). The biggest problem is probably the situation when optional parameter is moved to mandatory args: it demands much more refactoring on the call site. And, the special syntax might be weird (or neat? <span aria-label=\"big smile\" class=\"emoji emoji-1f604\" role=\"img\" title=\"big smile\">:big_smile:</span>), tho it emphasizes extra powers.</p>",
        "id": 425825274,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710123187
    },
    {
        "content": "<p>Omg, I edited the message a dozen times. It seems I believe in the idea more and more <span aria-label=\"big smile\" class=\"emoji emoji-1f604\" role=\"img\" title=\"big smile\">:big_smile:</span></p>",
        "id": 425830717,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710127360
    },
    {
        "content": "<p>However it’s just a decoupling. You still can pass defaults as a value but it’s more cumbersome with this design</p>",
        "id": 425851522,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710143375
    },
    {
        "content": "<blockquote>\n<p>It seems not intuitive to me though. Probably a bug, but I apologize for not working examples.</p>\n</blockquote>\n<p>I understand the confusion but this is expected behavior, given \"Destructuring is the only way to implement a record with optional fields. \".</p>",
        "id": 425864747,
        "sender_full_name": "Anton",
        "timestamp": 1710147986
    },
    {
        "content": "<p>I see. Then my only concern about the proposal is no way to bypass the record with optional fields</p>",
        "id": 425873345,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710151110
    },
    {
        "content": "<blockquote>\n<p>It seems not intuitive to me though. Probably a bug, but I apologize for not working examples.</p>\n</blockquote>\n<p>No worries, I had the same problem with the current optional fields! Thanks for taking the time to explain, lots of interesting things to unpack.</p>",
        "id": 425879084,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710153229
    },
    {
        "content": "<blockquote>\n<p>Destructuring is the only way to implement a record with optional fields.</p>\n</blockquote>\n<p>I know the rule, but it never clicked, hence the confusion. I understand why this is not possible:</p>\n<div class=\"codehilite\"><pre><span></span><code>x : { a ? Str }\nx.a\n</code></pre></div>\n<p>but if the record is protected by destructuring, I can't feel why this is forbidden:</p>\n<div class=\"codehilite\"><pre><span></span><code>x : { a ? Str } = if cond then {} else { a : &quot;foo&quot; }\n</code></pre></div>",
        "id": 425879154,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710153246
    },
    {
        "content": "<p>Passing the config through to an internal function I agree might be something you'd like to do. So there I'm more hesitant about the ritual of redefining the defaults as in the other cases with if and when.</p>",
        "id": 425879594,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710153397
    },
    {
        "content": "<p>Synctactically tying a special config record to the function is interesting. Though as you said, if we only use that for optional fields, moving a required field to be optional is a breaking change which feels undesirable.</p>",
        "id": 425879807,
        "sender_full_name": "Johannes Maas",
        "timestamp": 1710153473
    },
    {
        "content": "<p>Another concern regarding the limitation from above:</p>\n<div class=\"codehilite\"><pre><span></span><code># library\n\nfmt = \\str, { size ? 8, fontFamily ? &quot;sans-serif&quot; } -&gt; ...\n\n# app\n\nparagraph = [\n    (&quot;Lorem&quot;, { size : 20 }),\n    (&quot;ipsum&quot;, { fontFamily : &quot;Arial&quot; }),\n] |&gt; List.map \\(str, config) -&gt; fmt str config\n</code></pre></div>\n<p>There's no way to preserve the defaults as the library user so you have these options:</p>\n<ol>\n<li>add <code>fmt</code> call explicitly to each item in the list (probably the best choice here but I can imagine it won't work in some more complex scenarios)</li>\n<li>use tagged unions for your fields permutations</li>\n<li>introduce <code>fmtDefaults = { size : 8, fontFamily : \"sans-serif\" }</code>, extend each item in the list with the <code>fmtDefaults</code> and keep it up with the defaults in the imported module</li>\n<li>ask library owner to expose <code>fmtDefaults</code> and extend each item in the list with this record</li>\n<li>persuade the library owner to not use defaults (but have tagged unions instead) there because the function is commonly used for the edge case</li>\n</ol>\n<p>Yes, it's still a made-up example with a list. My concern is that this rule</p>\n<blockquote>\n<p>The ergonomics of destructuring mean this wouldn't be a good fit for data modeling, consider using a Result type instead.</p>\n</blockquote>\n<p>can be unknown ahead of time because arguments (destructuring) are part of the functionality provider, but the ergonomics problem happens on the consumer side (data modeling)</p>",
        "id": 425887624,
        "sender_full_name": "Kiryl Dziamura",
        "timestamp": 1710156239
    }
]