[
    {
        "content": "<p>It could be just me, but sometimes I have trouble identifying within a practically short enough instant of time exactly which tests have failed (I know the line numbers <em>are</em> displayed, but still), if the <code>roc test</code> output is large (due to the number of named values and/or their structure).</p>\n<p>To help with this, I've started defining a <code>name</code> value of implicit type <code>Str</code>, so that I can have that in the output, in case the corresponding test fails.</p>\n<p>Would it make sense for some related highlighting to be introduced. For example, the entire line could be printed in a different colour.</p>\n<div class=\"codehilite\" data-code-language=\"Elm\"><pre><span></span><code><span class=\"nv\">name</span><span class=\"nf\">:</span><span class=\"w\"> </span><span class=\"s\">\"Test #1 name\"</span>\n</code></pre></div>\n<p>A couple of options come to mind:</p>\n<ul>\n<li>Optionally utilising <code>name</code>(or equivalent) - the particular line displaying its value gets highlighted in the output (and it only gets output as it works currently - if the associated test fails). The corresponding \"unused definition\" warning should probably be suppressed.<ul>\n<li>Cons:<ul>\n<li>Whichever name gets picked (i.e., <code>name</code> or otherwise) to be the specially-treated one, it becomes a sort of a \"reserved\" name, associated with this proposed highlighting feature.</li>\n<li>This would probably make more sense to be introduced as a feature in association with top-level tests only.</li>\n</ul>\n</li>\n</ul>\n</li>\n<li>Introducing a <code>name</code> argument to <code>expect</code> or a parameters record - with optional values - argument to <code>expect</code> and running expectations will become:</li>\n</ul>\n<div class=\"codehilite\" data-code-language=\"Elm\"><pre><span></span><code><span class=\"nv\">expect</span><span class=\"w\"> </span><span class=\"p\">{}</span>\n<span class=\"w\">    </span><span class=\"nf\">...</span>\n</code></pre></div>\n<p>or</p>\n<div class=\"codehilite\" data-code-language=\"Elm\"><pre><span></span><code><span class=\"nv\">expect</span><span class=\"w\"> </span><span class=\"p\">{</span><span class=\"w\"> </span><span class=\"nv\">name</span><span class=\"nf\">:</span><span class=\"w\"> </span><span class=\"s\">\"Test #1 name\"</span><span class=\"w\"> </span><span class=\"p\">}</span>\n<span class=\"w\">    </span><span class=\"nf\">...</span>\n</code></pre></div>\n<p>- Pro: This would enable highlighting of inline expectations as well.</p>",
        "id": 431712025,
        "sender_full_name": "Hristo",
        "timestamp": 1712444304
    },
    {
        "content": "<p>Does a comment with a name or description above the expect work?</p>",
        "id": 431713592,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1712446157
    },
    {
        "content": "<p>I think the their was most tests didn't need names and they can be added with doc comments if wanted</p>",
        "id": 431713745,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1712446339
    },
    {
        "content": "<p>Yes, that's a great suggestion! It'd be substantially less disruptive.</p>",
        "id": 431714088,
        "sender_full_name": "Hristo",
        "timestamp": 1712446874
    }
]