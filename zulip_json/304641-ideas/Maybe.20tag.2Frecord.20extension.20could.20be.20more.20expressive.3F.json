[
    {
        "content": "<p>Right now we have some flexibility w/ regard to tag/record extensibility. We use the following syntax</p>\n<div class=\"codehilite\"><pre><span></span><code>TagA x : [ A ]x\nTagAB : [ B ]TagA\n\nRecordA x : { a : Str }x\nRecordAB : { b: : Str }RecordA\n</code></pre></div>\n<p>On the plus side, this really does help with extensibility and polymorphism when you need it. But one downside of how this is implemented is that (unless I'm missing something), you cannot easily extend multiple algebraic types in the same type definition.</p>\n<p>For example, I may want to write something like this:</p>\n<div class=\"codehilite\"><pre><span></span><code>TagA : [ A ]\nTagB : [ B ]\nTagC : [ C ]\n\nTagABC : [ ...TagA, ...TagB, ...TagC ]\n# TagABC : [ A, B, C ]\n</code></pre></div>\n<p>Technically we can do something like this:</p>\n<div class=\"codehilite\"><pre><span></span><code>Tag A x : [ A ]x\nTag B x : [ B ]x\nTag C x : [ C ]x\n\nTagABC : TagC (TagB (TagA []))\n</code></pre></div>\n<p>But that can get unwieldy very quickly, especially since each of the smaller tag unions is required to have its own type variable. And on top of that, this kind of composition doesn't currently allow for recursive structures, although perhaps that's technically possible and just not implemented yet.</p>\n<p>My main use case right now is to model an AST for a language parser. I want to be able to have something like this:</p>\n<div class=\"codehilite\"><pre><span></span><code>ArrayExpressionNode : [\n    ArrayExpression { loc : Location, elements : List ExpressionNode }\n]\n\nAssignmentExpressionNode : [\n    AssignmentExpression { loc : Location, operator : AssignmentOperator, left : [...PatternNode, ...ExpressionNode], right : ExpressionNode },\n]\n\nLogicalExpressionNode : [\n    LogicalExpression { loc: Location, operator : LogicalOperator, left : ExpressionNode, right : ExpressionNode },\n]\n\nExpressionNode : [\n    ...ArrayExpressionNode,\n    ...AssignmentExpressionNode,\n    ...LogicalExpressionNode,\n]\n</code></pre></div>\n<p>As it stands, I can almost accomplish this using the following tag-extension syntax:</p>\n<div class=\"codehilite\"><pre><span></span><code>ArrayExpressionNode a : [\n    ArrayExpression { loc : Location, elements : List ExpressionNode }\n]a\n\nAssignmentExpressionNode a : [\n    AssignmentExpression { loc : Location, operator : AssignmentOperator, left : [Pattern PatternNode, Expression ExpressionNode], right : ExpressionNode },\n]a\n\nLogicalExpressionNode a : [\n    LogicalExpression { loc : Location, operator : LogicalOperator, left : ExpressionNode, right : ExpressionNode },\n]a\n\nExpressionNode : ArrayExpressionNode (AssignmentExpressionNode (LogicalExpressionNode []))\n</code></pre></div>\n<p>But the recursive reference to <code>ExpressionNode</code> within the other three tag union definitions seems to cause the compiler to crash. I know there are some seemingly intentional restrictions on type-level recursion in Roc, and perhaps those restrictions will always remain in place. But I cannot seem to find a way to produce the recursive type structure that I need. For example, this fails with a stack overflow at compile time:</p>\n<div class=\"codehilite\"><pre><span></span><code>ArrayExpressionNode : [\n  ArrayExpression { loc : Location, elements : List ExpressionNode }\n]\n\nAssignmentExpressionNode : [\n    AssignmentExpression { loc : Location, operator : AssignmentOperator, left : [Pattern PatternNode, Expression ExpressionNode], right : ExpressionNode },\n]\n\nLogicalExpressionNode : [\n    LogicalExpression { loc : Location, operator : LogicalOperator, left : ExpressionNode, right : ExpressionNode },\n]\n\nExpressionNode : [\n    ArrayExpression ArrayExpressionNode,\n    AssignmentExpression AssignmentExpressionNode,\n    LogicalExpression LogicalExpressionNode,\n]\n</code></pre></div>\n<p>In the above example, it seems that the only recursive reference to <code>ExpressionNode</code> that doesn't cause the compiler to crash is the <code>List ExpressionNode</code> type located within <code>ArrayExpressionNode</code>. Tag wrappers like <code>[Pattern PatternNode, Expression ExpressionNode]</code> actually cause compiler crashes. I assume this isn't intentional. And the recursion issue probably deserves its own topic.</p>\n<p>But regardless, even if the above code worked, it is still somewhat annoying to have to match on a pattern like this:</p>\n<div class=\"codehilite\"><pre><span></span><code>get_loc : ExpressionNode -&gt; Location\nget_loc = |expression|\n    when expression is\n        ArrayExpression(ArrayExpression({ loc })) -&gt; loc\n        AssignmentExpression(AssignmentExpression({ loc })) -&gt; loc\n        LogicalExpression(LogicalExpression({ loc })) -&gt; loc\n</code></pre></div>\n<p>And that excessive boilerplate is what I hope Roc can help me avoid eventually...</p>",
        "id": 530425604,
        "sender_full_name": "Austin Davis",
        "timestamp": 1753301618
    },
    {
        "content": "<p>Sorry for the long-winded post, by the way. This has been marinating in my brain for a while now. <span aria-label=\"sweat smile\" class=\"emoji emoji-1f605\" role=\"img\" title=\"sweat smile\">:sweat_smile:</span></p>",
        "id": 530427967,
        "sender_full_name": "Austin Davis",
        "timestamp": 1753302636
    },
    {
        "content": "<p>Also, I should say that I can currently work around this problem by simply duplicating a lot of type signatures (which I think is probably unnecessary from a technical standpoint):</p>\n<div class=\"codehilite\"><pre><span></span><code>ArrayExpressionNode : [\n    ArrayExpression { loc : Location, elements : List ExpressionNode }\n]\n\nAssignmentExpressionNode : [\n    AssignmentExpression {loc: Location, operator : AssignmentOperator, left : [Pattern PatternNode, Expression ExpressionNode], right : ExpressionNode },\n]\n\nLogicalExpressionNode : [\n    LogicalExpression { loc : Location, operator : LogicalOperator, left : ExpressionNode, right : ExpressionNode },\n]\n\nExpressionNode : [\n    ArrayExpression { loc : Location, elements : List ExpressionNode },\n    AssignmentExpression {loc: Location, operator : AssignmentOperator, left : [Pattern PatternNode, Expression ExpressionNode], right : ExpressionNode },\n    LogicalExpression { loc : Location, operator : LogicalOperator, left : ExpressionNode, right : ExpressionNode },\n]\n\nget_loc : ExpressionNode -&gt; Location\nget_loc = |expression|\n    when expression is\n        ArrayExpression({ loc, elements }) -&gt; loc\n        AssignmentExpression({ loc, operator, left, right }) -&gt; loc\n        LogicalExpression({ loc, operator, left, right }) -&gt; loc\n</code></pre></div>\n<p>This gives me a way to have aliases for individual tags as well as an alias for larger tag unions that contain those individual tags. This avoids type-level mutual recursion, so it doesn't cause a compiler crash, and it's a relatively easy API to consume.</p>\n<p>But as you can see, it requires manually ensuring that all the duplicated types remain identical to each other, which is not ideal from a maintenance standpoint.</p>",
        "id": 530431130,
        "sender_full_name": "Austin Davis",
        "timestamp": 1753304013
    },
    {
        "content": "<p>Two fly by comments as I haven't had time to dive into this fully:</p>\n<ol>\n<li>We plan to switch to the <code>[A, B, ...SomeTag]</code> syntax for expanding tags</li>\n<li>In the future all recursive data structures will require being through nominal types. Which require being explicit and do not allow expansion. With the only exception maybe being List based recursion (and maybe box, but that requires a null case, so it not as clear if it requires being nominal or not).</li>\n</ol>",
        "id": 530431163,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1753304023
    },
    {
        "content": "<p>Oh, and get_loc should work with <code>ArrayExpression({ loc, ...}) | Assignment expression({ loc, ...}) | ...</code></p>\n<p>Or something a long those lines</p>",
        "id": 530431417,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1753304133
    },
    {
        "content": "<p>Interesting... I do like that approach. So with the new design, could I do something like this?</p>\n<div class=\"codehilite\"><pre><span></span><code>TagA : [ A { c : NominalC } ]\nTagB : [ B { c : NominalC } ]\nTagC : [...A, ...B]\nNominalC := TagC\n\nunwrap_nominal_c : NominalC -&gt; TagC\nunwrap_nominal_c = |@NominalC(nominal_c)|\n    nominal_c\n\n# Consumer usage:\n\ninner_c = when unwrap_nominal_c(nominal_c) is\n    A({c}) | B({c}) -&gt; c\n</code></pre></div>",
        "id": 530432778,
        "sender_full_name": "Austin Davis",
        "timestamp": 1753304699
    },
    {
        "content": "<p>I believe so. Not sure all the restrictions related to recursion and declaration, but looks good to me</p>",
        "id": 530434609,
        "sender_full_name": "Brendan Hansknecht",
        "timestamp": 1753305408
    }
]