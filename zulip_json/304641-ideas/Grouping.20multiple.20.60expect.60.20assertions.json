[
    {
        "content": "<p>Sometimes I want to check multiple values in an <code>expect</code> call. Usually this happens when I have a complex record where I want to assert a few attributes.</p>\n<div class=\"codehilite\"><pre><span></span><code>expect\n    result = parse &quot;some complex string that I need to parse&quot;\n    result.name == &quot;a&quot; &amp;&amp; result.values == [1, 2, 3] &amp;&amp; result.direction == North\n</code></pre></div>\n<p>Imagine there's a 4th attribute here I don't want to test so I can't do <code>== { ... }</code>.</p>\n<p>Doing <code>&amp;&amp;</code> here is fine, but it gets cumbersome if I add more assertions. I could split this up into multiple <code>expect</code> calls but then duplicating the test setup can get verbose.</p>\n<p>What I'd like is a way to group test code and use <code>expect</code> for each assertion. Perhaps with a <code>test</code> keyword.</p>\n<div class=\"codehilite\"><pre><span></span><code>test\n    result = parse &quot;some complex string that I need to parse&quot;\n    expect result.name == &quot;a&quot;\n    expect result.values == [1, 2, 3]\n    expect result.direction == North\n</code></pre></div>\n<p>Thoughts?</p>",
        "id": 409241900,
        "sender_full_name": "Ryan Bates",
        "timestamp": 1703097861
    }
]